/**
 * Canonical FrameTrail brand colors for TS-injected styles (toolbar, region
 * capture). CSS files cannot import TS, so assets/tailwind.css (--brand /
 * --recording) and entrypoints/snapshot-shield/style.css keep literal copies;
 * change them together.
 */
export const BRAND_ACCENT = '#7094f4';
export const BRAND_DANGER = '#ff4747';
